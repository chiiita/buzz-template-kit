---
name: buzz-template-add
description: バズ型テンプレ集（GitHub chiiita/buzz-template-kit・公開 https://buzz-template-kit.vercel.app）に、参考画像をもとに新しい型を追加するスキル。「バズ型に〇〇を追加」「この参考画像で型を作って」「テンプレ集に型/テーマ追加」「buzz-template-kitに追加」等を検知したら最初に開く。ヒアリング→構造分解→実装→QA目視→デプロイ案内まで一気通貫。
---

# バズ型テンプレ集 — 型追加スキル

参考バズIG投稿の画像をもとに、HTMLテンプレ型を1つ追加し、公開URLを更新するまでの一気通貫フロー。

- **プロジェクト**：`/Volumes/CIRAGO/クラウドコード/バズ型テンプレ集/`
- **公開**：https://buzz-template-kit.vercel.app ／ **GitHub**：chiiita/buzz-template-kit
- 規模：73型（表紙21/中身32/締め20）×28テーマ

## STEP 0：技術コンテキストを読む（必須・最初）
`/Volumes/CIRAGO/クラウドコード/バズ型テンプレ集/CLAUDE.md` を Read。
＝型の追加パターン／satori制約／フィールド型／共通ヘルパー／コマンドの正本。

## STEP 1：ヒアリング（不足分だけ AskUserQuestion で聞く・分かってる事は聞かない）
1. **参考画像**：どこにある? ＝ファイルパス／チャットに貼る／無ければ「Pinterestで探す」(STEP2の収集へ)
2. **カテゴリ**：表紙 / 中身 / 締め のどれ?
3. **用途・狙い**：何を見せる型? （手順・比較・診断・告知・データ・引用・ランキング 等）
4. **テーマ**：既存28テーマでOK? 新しい配色が要る?（要るならTHEMESに1行追加）

## STEP 2：構造分解
- 参考画像を **View（Read）して分解**：レイアウト／配色／文字の階層／装飾／写真有無
- 既存の似た型がないか `build_templates.mjs` の TEMPLATES を確認（重複回避・流用）
- （任意）`node pinterest_collect.mjs "検索語" design_kb/references/NN_xxx 8` で追加収集

## STEP 3：実装（build_templates.mjs の TEMPLATES に1要素追加）
```js
{ id:'content_xxx', name:'○○（中身）', cat:'中身',     // cat='表紙'|'中身'|'締め'
  fields:[{key:'title',label:'タイトル（改行で折る）',def:'...'},
          {key:'items',label:'項目',type:'rows',cols:['見出し','説明'],def:'a｜b\nc｜d'}],
  render:(d,t)=>wrap({bg:t.bg,color:t.ink,font:t.body,align:'stretch'},`...`) },
```
- 文章＝`nl(d.x)`／リスト＝`type:'rows',cols:[...]`（区切り全角｜）／アイコン＝`type:'icon'`／写真＝`type:'file'`／選択＝`type:'select'`
- テーマトークン：`t.bg/ink/sub/line/accent/accentDeep/onAccent/panel/panelSoft/head/body/display/darkBg/darkInk/darkAccent/darkSub`
- **satori制約厳守**（CLAUDE.md）：複数child div=flex必須／flexルート直下に絶対配置複数=不可／border-styleはsolid/dashedのみ(dotted不可)／SVG内textは描画されない／✓✕は`icon('check')/icon('x')`
- renderで新ヘルパー/定数を使うなら**ブラウザ埋込行にも追加**（忘れるとプレビュー真っ黒）

## STEP 4：ビルド＆QA目視（必須）
```bash
cd "/Volumes/CIRAGO/クラウドコード/バズ型テンプレ集"
node build_templates.mjs
```
→ `dist/QA/<新id>.png` を **Read して目視**：崩れ・はみ出し・豆腐・**薬機法/誇大表現（効く・必ず・治る・改善 等）**が無いか。テーマ別は `node build_templates.mjs --deck deck.json out.pdf --png` で確認。NGは直して再ビルド、OKまで反復。

## STEP 5：提示 → 承認 → デプロイ案内
- ユーザーにQA画像を提示し**承認を取る**
- OKなら、**ユーザー端末で実行する更新コマンド**を案内（当環境はTLS遮断でpush不可）：
  ```
  cd "/Volumes/CIRAGO/クラウドコード/バズ型テンプレ集"
  git add -A
  git commit -m "add template: <型名>"
  git push
  ```
  → Vercel自動デプロイ→公開URLに反映（数十秒）。利用者は再読み込みだけ。

## 補足
- アプリ左リストの A/B/C＋連番は**配列順から自動表示**（手動採番不要）
- commit author＝`chiiita <61923324+chiiita@users.noreply.github.com>`固定（git -cで別人指定しない）
- 顔図解の顔バリエ＝`faceSVG(cf,cc,cj,variant)`／袋文字＝`outline()`／改行制御＝`nl()`
